<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#121212">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:id="@+id/statusText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="ShadowBrain: Ready"
            android:textColor="#00FF00"
            android:textSize="18sp"
            android:layout_marginBottom="16dp" />

        <com.shadow.shadowbrain.ui.PixelGridView
            android:id="@+id/gridInput"
            android:layout_width="280dp"
            android:layout_height="280dp"
            android:layout_gravity="center"
            android:layout_marginBottom="16dp" />

        <Spinner
            android:id="@+id/labelSpinner"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:background="#333333"
            android:layout_marginBottom="16dp" />

        <Button
            android:id="@+id/btnAddSample"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="1. ДОДАТИ / HARVEST (LONG)"
            android:backgroundTint="#2E7D32" />

        <Button
            android:id="@+id/btnTrainBatch"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="2. ВЧИТИ 5 ЕПОХ / RESET (LONG)"
            android:backgroundTint="#4527A0"
            android:layout_marginTop="8dp" />

        <Button
            android:id="@+id/btnPredict"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="3. ПЕРЕВІРИТИ (PREDICT)"
            android:backgroundTint="#1565C0"
            android:layout_marginTop="8dp" />

        <Button
            android:id="@+id/btnClear"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="СТОП / ОЧИСТИТИ"
            android:backgroundTint="#37474F"
            android:layout_marginTop="24dp" />

    </LinearLayout>
</ScrollView>
